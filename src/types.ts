export type Env = {
    [key: string]: string;
}

export type ImageParams = {
    [key: string]: any;
}

export type CacheParams = {
    [key: string]: any;
}

export type MIMEPair = {
    ext: string;
    mime: string;
}